import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const reusableMovieCard = () => {
  return (
    <View>
      <Text>reusableMovieCard</Text>
    </View>
  );
};

export default reusableMovieCard;

const styles = StyleSheet.create({});
